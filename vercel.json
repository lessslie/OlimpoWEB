{
  "rewrites": [
    { "source": "/(.*)", "destination": "/front/$1" }
  ],
  "buildCommand": "cd front && yarn install && yarn build",
  "outputDirectory": "front/dist",
  "installCommand": "cd front && yarn install",
  "framework": "vite"
}
